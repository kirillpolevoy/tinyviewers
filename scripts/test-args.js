import dotenv from "dotenv"; import path from "path"; import { fileURLToPath } from "url"; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); dotenv.config({ path: path.join(__dirname, "..", ".env.local") }); console.log("🎯 Testing Frozen filtering"); console.log("Command line args:", process.argv); console.log("Specific movie:", process.argv[2]);
